<?xml version="1.0" encoding="UTF-8"?>

<?jelly escape-by-default='true'?>

<jelly:jelly xmlns:jelly="jelly:core" xmlns:form="/lib/form" xmlns:credentials="/lib/credentials">
	<form:entry field="name" title="${%doktor.config.ConfluenceServer.name}">
		<form:textbox/>
	</form:entry>
	<form:entry field="url" title="${%doktor.config.ConfluenceServer.url}">
		<form:textbox/>
	</form:entry>
	<form:entry field="credentials" title="${%doktor.config.ConfluenceServer.credentials}">
		<credentials:select/>
	</form:entry>
</jelly:jelly>
