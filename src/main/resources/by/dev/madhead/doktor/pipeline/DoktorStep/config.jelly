<?xml version="1.0" encoding="UTF-8"?>

<?jelly escape-by-default='true'?>

<jelly:jelly xmlns:jelly="jelly:core" xmlns:form="/lib/form">
	<form:entry field="server" title="${%doktor.pipeline.DoktorStep.server}">
		<form:select/>
	</form:entry>
	<form:entry field="markdownIncludePattern" title="${%doktor.pipeline.DoktorStep.markdownIncludePattern}">
		<form:textbox/>
	</form:entry>
	<form:entry field="markdownExcludePattern" title="${%doktor.pipeline.DoktorStep.markdownExcludePattern}">
		<form:textbox/>
	</form:entry>
	<form:entry field="asciidocIncludePattern" title="${%doktor.pipeline.DoktorStep.asciidocIncludePattern}">
		<form:textbox/>
	</form:entry>
	<form:entry field="asciidocExcludePattern" title="${%doktor.pipeline.DoktorStep.asciidocExcludePattern}">
		<form:textbox/>
	</form:entry>
</jelly:jelly>
